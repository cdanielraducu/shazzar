<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Required on Android 13+ (API 33) to show push/local notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required to schedule exact alarms for habit reminders.
         On API 31+ the user must grant this in Settings > Special app access > Alarms. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Required to receive the BOOT_COMPLETED broadcast for re-registering alarms after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Health Connect — step count read access -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />

    <!-- Health Connect library requires minSdk 26 but our app targets 21.
         We handle the API level check at runtime in HealthModule.kt —
         isAvailable() returns false on API < 26, all other methods reject early. -->
    <uses-sdk tools:overrideLibrary="androidx.health.connect.client" />

    <!-- Needed so PackageManager can resolve Health Connect -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="shazzar" />
        </intent-filter>
      </activity>

      <!-- Receives AlarmManager broadcasts to fire local notifications -->
      <receiver
        android:name=".modules.notifications.NotificationReceiver"
        android:exported="false" />

      <!-- Re-registers AlarmManager alarms after device reboot -->
      <receiver
        android:name=".modules.notifications.BootReceiver"
        android:exported="false">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
        </intent-filter>
      </receiver>

      <!-- Receives FCM tokens and messages directly from the Firebase SDK -->
      <service
        android:name=".modules.notifications.ShazzarFirebaseMessagingService"
        android:exported="false">
        <intent-filter>
          <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
      </service>

      <!-- Health Connect requires apps to declare this intent filter so the
           system knows to list them under "App permissions". Without it,
           Health Connect shows "No compatible apps installed".
           On API 34+ this also enables the platform permission request flow.
           The activity itself just needs to exist — we point it at MainActivity
           since we handle everything in the RN bridge layer. -->
      <activity
        android:name=".HealthPermissionRationaleActivity"
        android:exported="true">
        <intent-filter>
          <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent-filter>
      </activity>

      <!-- API 34+ uses a different action name for the same purpose -->
      <activity-alias
        android:name="ViewPermissionUsageActivity"
        android:exported="true"
        android:targetActivity=".HealthPermissionRationaleActivity"
        android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
        <intent-filter>
          <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
          <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
        </intent-filter>
      </activity-alias>
    </application>
</manifest>
